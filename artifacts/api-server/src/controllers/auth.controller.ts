/**
 * Auth controller
 *
 * Routes (wired in routes/auth.ts):
 *   POST /api/auth/register     — email + password sign-up
 *   POST /api/auth/login        — email + password sign-in
 *   POST /api/auth/request-otp  — phone OTP send
 *   POST /api/auth/verify-otp   — phone OTP verify + JWT
 *   GET  /api/auth/me           — current user profile
 */

export {};
