/**
 * Reports controller
 *
 * Routes (wired in routes/reports.ts):
 *   POST /api/reports — submit a content violation report
 *
 * Rules:
 *   - one report per (reporter, auction) pair
 *   - reporter cannot be the auction seller
 */

export {};
