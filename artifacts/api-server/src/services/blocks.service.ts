/**
 * Blocks service
 *
 * Manages user block relationships.
 *
 * Planned functions:
 *   blockUser(blockerId, blockedId)
 *   unblockUser(blockerId, blockedId)
 *   getBlockedUserIds(blockerId)  — used by feed queries to exclude blocked sellers
 *   isBlocked(blockerId, blockedId)
 */

export {};
