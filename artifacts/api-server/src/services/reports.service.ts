/**
 * Reports service
 *
 * Handles policy violation reporting and moderation queue.
 *
 * Planned functions:
 *   createReport(reporterId, auctionId, reason, details?)
 *   getPendingReports(limit?, cursor?)             — admin use
 *   resolveReport(reportId, adminId, action, note) — admin use
 *   queueForModeration(auctionId, source)
 */

export {};
