<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ─── Permissions ──────────────────────────────────────────────── -->

    <!-- Google Play Billing (subscriptions / in-app purchases) -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Network (required for all API calls and media streaming) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera (photo/video uploads) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Media read — Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Media read — Android 12 and below (API ≤ 32) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Push notifications — Android 13+ (API 33+) requires explicit grant -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibrate on notification receipt -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Location (for auction geo-filtering) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ─── Implicit intent visibility (Android 11+ package visibility) ─ -->
    <!-- Required to open WhatsApp share links and detect installed apps  -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <!-- App launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                OAuth deep-link callback handler.
                When Google / Supabase redirect to com.bidreel.app://auth/callback,
                Android routes the intent here (launchMode="singleTask" brings the
                existing activity to the foreground rather than creating a new one).
                The Capacitor bridge then fires the "appUrlOpen" JS event so the
                web layer can exchange the auth code / tokens with Supabase.
            -->
            <intent-filter android:label="BidReel OAuth Callback">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.bidreel.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ─── Firebase Cloud Messaging defaults ────────────────────────────
             These three meta-data tags are REQUIRED for reliable shade
             rendering on Android 8+. Without them:
               • the FCM payload `android.notification.icon` had to resolve
                 to a real drawable — `ic_launcher` is a mipmap and many
                 OEM ROMs silently DROP the notification when the lookup
                 fails.
               • Android creates an auto-generated channel called
                 "Miscellaneous" which users disable, killing all pushes.
             We point at:
               • drawable/ic_stat_notify  (white silhouette, transparent bg)
               • color/notification_color (BidReel violet)
               • string/default_notification_channel_id = "bidreel_default"
             The server's FCM payload uses channelId "bidreel_default" — they
             must match for system-shade rendering to work.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
    </application>

</manifest>
