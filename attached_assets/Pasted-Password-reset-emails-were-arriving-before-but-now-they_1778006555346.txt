Password reset emails were arriving before, but now they stopped arriving.

We need to implement a controlled password reset email limit:

Goal:
Allow each account/email to request up to 3 password reset emails within 24 hours.
After 3 requests, block further reset emails for 24 hours.

Requirements:

1. Inspect the current forgot-password flow:

   * ForgotPasswordModal
   * Supabase resetPasswordForEmail
   * reset-password page
   * any backend auth routes

2. Determine why emails stopped arriving:

   * Supabase default SMTP rate limit
   * missing custom SMTP
   * redirect issue
   * Supabase API error being swallowed

3. Add proper logging:

   * Log Supabase resetPasswordForEmail errors to console/server logs
   * Do not expose whether the email exists to the user

4. Implement rate limiting per email/account:

   * Max 3 reset email requests per 24 hours
   * After 3 attempts, return a safe generic message
   * Store attempts in a backend table, not frontend state
   * Normalize email to lowercase before counting
   * Do not reveal whether the email exists

5. Recommended backend flow:

   * Frontend calls our backend endpoint `/auth/forgot-password`
   * Backend checks/reset request count
   * If under limit, calls Supabase Admin/Auth reset password email
   * If over limit, do not call Supabase
   * Always return a generic success response to the frontend:
     “If this email exists, a reset link has been sent.”

6. Add a table if needed:
   `password_reset_requests`
   fields:

   * id
   * email_hash or normalized_email
   * request_count
   * window_start
   * blocked_until
   * created_at
   * updated_at

7. Security:

   * Do not store raw email if hashing is easy to implement
   * Do not leak account existence
   * Do not allow frontend bypass
   * Keep reset page PKCE fix intact

8. Also confirm whether Supabase custom SMTP is needed.
   If Supabase default email service is rate-limiting the project, explain that we need Resend/SendGrid/Mailgun/Postmark SMTP for production.

Do not touch unrelated features.
After implementation, summarize files changed and how to test:

* first 3 requests should send
* 4th request within 24h should not send
* user sees generic success message
* after 24h it works again
