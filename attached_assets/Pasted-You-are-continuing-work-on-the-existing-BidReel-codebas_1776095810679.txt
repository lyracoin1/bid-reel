You are continuing work on the existing BidReel codebase.

Important global rules:
- First inspect the current implementation and KEEP everything that already matches the requirements.
- DO NOT rebuild working parts.
- DO NOT introduce live bidding, websockets, realtime subscriptions, payments, shipping, commissions, checkout, delivery tracking, or order management.
- If something already exists and is correct, skip it.
- Only implement what is missing, broken, inconsistent, or incomplete.
- Keep BidReel as a connection layer only between users.
- Do not introduce any product wording or architecture that implies platform responsibility for payment, shipping, delivery, disputes, or escrow.

Project direction:
BidReel should behave as a short-video auction platform that only connects users with each other.
The platform is NOT a payment intermediary, NOT a shipping intermediary, and does NOT take a percentage of completed deals.
After an auction ends, the app should simply mark it as ended and show the result. Any communication between users happens directly, outside platform responsibility.

Current context:
- Main app: React + Vite
- Admin panel: separate React app
- Backend: Vercel serverless API
- Database/Auth: Supabase
- Storage: Cloudflare R2
- Auth has already been migrated to email-first
- Admin/API production routing was already fixed
- Admin preview/emulator was already audited and upgraded
- This phase is ONLY about auditing the existing schema and aligning it safely with the simplified auction product model

Your task in this phase:
A) Audit the existing implementation first
Inspect the current database schema, migrations, API routes, frontend screens, upload flow, and auction logic.

Requirements for the audit:
- Reuse any existing tables, fields, components, and patterns that already fit the required product direction
- If existing names differ but are still valid, adapt carefully instead of duplicating structures
- Preserve any working auth/profile flows unless they conflict directly with the new simplified auction model
- Produce a concise implementation plan before changing code
- Clearly identify what already exists and what should be kept untouched

B) Align the schema with the simplified auction model
Create or adapt the minimum Supabase tables/fields needed for this product direction.

Required core tables and fields:

1) profiles
Needed fields:
- id (uuid, linked to auth.users.id)
- email
- username (unique)
- display_name
- avatar_url
- phone
- whatsapp_number
- bio
- is_active
- created_at
- updated_at

2) auctions
Needed fields:
- id
- seller_id
- title
- description
- start_price
- current_price
- min_increment
- currency
- status (draft, active, ended, cancelled only)
- starts_at
- ends_at
- winner_id (nullable)
- winner_bid_id (nullable)
- bids_count
- views_count
- contact_unlocked
- created_at
- updated_at

3) auction_media
Needed fields:
- id
- auction_id
- media_type
- storage_path_original
- storage_path_compressed
- playback_url
- thumbnail_url
- duration_seconds
- width
- height
- file_size_bytes
- sort_order
- created_at

4) bids
Needed fields:
- id
- auction_id
- bidder_id
- amount
- is_winning
- created_at

5) notifications
Needed fields:
- id
- user_id
- type
- title
- body
- auction_id (nullable)
- read_at (nullable)
- created_at

6) favorites
Needed fields:
- id
- user_id
- auction_id
- created_at

7) reports
Needed fields:
- id
- reporter_id
- auction_id (nullable)
- reported_user_id (nullable)
- reason
- details
- status
- created_at

Optional if useful:
8) auction_status_logs
- id
- auction_id
- old_status
- new_status
- note
- created_at

Schema requirements:
- Add proper foreign keys, indexes, and constraints
- Keep the schema as simple as possible
- Add or adjust Row Level Security policies safely
- Do not break existing auth/profile flows
- If a profiles trigger already exists, preserve it unless it conflicts with the new model
- Avoid duplicate tables or duplicate state models

C) Migration discipline
- If migrations are needed, create them carefully and make them safe
- Prefer additive or compatibility-preserving changes when possible
- Do not rename or break working APIs unless absolutely necessary
- Reuse current patterns in the codebase where possible
- Skip anything already implemented correctly

D) Deliverables required from this phase
After finishing, provide:
1. A short audit summary of what already existed and what you skipped
2. The exact missing pieces you implemented
3. Any new migrations in full
4. Any environment variables or deployment steps required
5. A final summary of the aligned schema and how it supports the simplified auction lifecycle

Important:
Do not implement bidding logic, automatic closure, WhatsApp button, video compression, or UI wording changes in this phase unless some tiny compatibility adjustment is absolutely required for schema consistency.
This phase is schema audit + schema alignment only.